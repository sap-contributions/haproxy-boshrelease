set -euxo pipefail

source aws-lc-versions.sh

# Install prebuilt cmake binary tarball — no compile step needed.
tar xzf haproxy/cmake-${CMAKE_VERSION}-linux-x86_64.tar.gz
cp -r cmake-${CMAKE_VERSION}-linux-x86_64/. ${BOSH_INSTALL_TARGET}/
